{
  "name": "worldchef-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "npm --workspace backend run dev",
    "start": "npm --workspace backend run start",
    "test": "npm --workspaces test && deno task test",
    "test:ci": "npm --workspaces test && deno task test",
    "test:workspace": "npm --workspaces test",
    "test:deno": "deno task test"
  }
}
