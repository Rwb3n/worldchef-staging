{
  "name": "worldchef-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "npm --workspace backend run dev",
    "start": "npm --workspace backend run start",
    "test": "npm --workspaces test",
    "test:ci": "npm --workspaces test"
  }
}
