{
  "name": "worldchef-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "yarn workspace backend run dev",
    "start": "yarn workspace backend run start",
    "build": "yarn workspace backend run build",
    "start:prod": "yarn workspace backend run start:prod",
    "postinstall": "yarn workspace backend run build",
    "test": "yarn workspaces run test && deno task test",
    "test:ci": "yarn workspaces run test && deno task test",
    "test:workspace": "yarn workspaces run test",
    "test:deno": "deno task test"
  }
}
